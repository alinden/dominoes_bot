package game

import org.scalatest.flatspec.AnyFlatSpec

class PlayerSpec extends AnyFlatSpec {

  "A Player" should "be created" in {
    // val tiles = List(Domino(2,1),Domino(4,4))
    // val x = Player("a",tiles)
    // assert(x.name == "a")
    // assert(x.hand == tiles)
  }
}
